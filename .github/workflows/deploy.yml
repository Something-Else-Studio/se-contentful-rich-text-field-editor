on: [push]

jobs:
  deploy_job:
    runs-on: ubuntu-latest
    name: Deploy app to Contentful
    if: contains(github.ref, 'main') || contains(github.ref, 'master')
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js 20.x
        uses: actions/setup-node@v3
        with:
          node-version: 20.x
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.15.1
      - run: pnpm install --frozen-lockfile
      - run: pnpm run build
      - uses: contentful/actions-app-deploy@v1
        with:
          organization-id: ${{ env.CONTENTFUL_ORG_ID }}
          app-definition-id: ${{ env.CONTENTFUL_APP_DEF_ID }}
          access-token: ${{ secrets.CONTENTFUL_ACCESS_TOKEN }}
          folder: build
